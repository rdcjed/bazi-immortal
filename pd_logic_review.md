# PD 逻辑漏洞报告

## 项目：bazi-immortal 命理推算逻辑审查

审查日期：2026-07-03
审查范围：wuxing.py, predictions.py, shensha.py, dayun.py, calculator.py, constants.py, shisheng.py, contextual.py, knowledge_loader.py, prompts/

---

### 🔴 严重逻辑问题

#### 1. 从格检测双层逻辑不一致 (wuxing.py)

`analyze_ri_zuo_strong_weak()` (L311-316) 和 `analyze_ge_ju()` (L460-502) 两处存在不同的从格判断逻辑，且前者可能错误覆盖后者：

- **analyze_ri_zuo_strong_weak**: 使用 `helping_raw >= 8` 和 `harming_count >= 8` 的粗暴阈值
  - `helping_raw = helping_count + (3 if has_strong_root else 0)` 这个公式使'score'容易被堆高导致误判为"从强"
  - `harming_count >= 8` 作为从弱条件，但总天干只有4个，加上藏干权重后≥8极罕见，导致从弱几乎永远不会触发
- **analyze_ge_ju**: 使用百分比阈值（印比≥75%从强，官杀/财/食伤≥45%从其他），两者不一致
- **核心问题**: analyze_ri_zuo_strong_weak 判定"从强"后，strength["strong_weak"] 变成 "从强"，这会影响后续 predictions.py 的评分系统。但 analyze_ge_ju 有自己独立的判断逻辑，两套逻辑结果可能冲突。

**建议**: 统一为单一从格检测逻辑。analyze_ri_zuo_strong_weak 只做强弱判断（身强/身弱/偏强/偏弱/中和），不从格。analyze_ge_ju 作为格局判断的权威来源。

#### 2. 化气格月令验证过于严格 (wuxing.py L537)

```python
if month_zhi in need_zhi:  # only passes if month is exactly the 当令 month
```

传统命理中化气格成功的条件包括但不限于：
- **月令得化神之气**：不一定要月支恰好是某个固定地支。如甲己化土，月令在巳午（火旺生土）也能助化
- **化神透干**：化出的五行在天干上出现（如甲己化土，天干有戊己土透出），极大增加化气成功率
- **地支有化神之根**：化气五行在地支有强根

当前代码只检查 `month_zhi in need_zhi`（即甲己化土只允许辰戌丑未月），漏掉了大量可能的化气格：
- 甲己化土：巳午月（火生土）也应考虑
- 乙庚化金：辰戌丑未月（土生金）也应考虑
- 丙辛化水：申酉月（金生水）也应考虑
- 丁壬化木：亥子月（水生木）也应考虑
- 戊癸化火：寅卯月（木生火）也应考虑

#### 3. 缺失"调候用神"逻辑 (全系统)

Prompt 文件 (L129-135) 明确提到调候用神的重要性：
```
⭐ **调候用神（穷通宝鉴法，重要补充）：**
   - 夏生先看需不用水調候（丙丁火夏日宜有水）
   - 冬生先看需不用火調候（壬癸水冬月宜有火暖）
   - 调候用神与强弱用神冲突时，**调候优先**
```

但**代码中完全没有实现调候用神逻辑**。wuxing.py 的用神判断完全基于五行强弱（身强用克泄耗，身弱用生扶），没有考虑穷通宝鉴的调候法则。

后果：夏生火旺之命（如丙丁火午月生）可能被判定为"从强"或"身强"，用神定为金水（克泄耗）。但传统调候法认为夏火需用水調候，这与强弱用神可能在方向上一致，但理由完全不同，且对某些八字判断会根本不同。

#### 4. 缺失"空亡"逻辑 (shensha.py + 全系统)

六十甲子旬空（六甲空亡）是命理推算中极为重要的概念，影响到用神判断、大运流年吉凶等。但：
- shensha.py 中列出了六甲空亡(L279-283)但其 `rule: {}` 没有实现实际的推算逻辑
- `find_shen_sha()` 中没有任何处理该神煞的分支
- 整个系统中没有任何空亡计算逻辑
- 注意 constants.py 里也没有空亡相关的数据表

传统上，空亡会：
- 让空亡地支对应的十神力量减弱
- 大运/流年填实空亡时，对应领域发生重大变化
- 影响用神判断的准确性

---

### 🟡 中等逻辑问题

#### 5. 从格缺失"从势格"和变体 (wuxing.py)

当前从格检测只覆盖四种：从强、从官、从财、从儿。命理学中常见但缺失的从格类型：

- **从势格**：日主极弱，但四柱中多种克泄耗混杂（官杀+财+食伤都有，但没有单一类别≥45%），此时"从势"而非从某一种。当前代码会认为是一般身弱。
- **假从格**：当前 analyze_ge_ju 只有从强区分了"假从强格"（75-85%），其他从格没有假从分支。传统上从官/从财/从儿也有假从，如日主有一点点微根但整体仍以从论。

#### 6. 从格阈值可能过高/过低 (wuxing.py)

- 从强≥75%：传统上从强需要日主极强，全局几乎都是印比。75%阈值在8字计分体系中（天干×4+地支本气×4+藏干≈14-16个计分项），75%相当于12/16，偏高但还算合理。
- 从官/从财/从儿≥45%：在4天干+4地支体系中，45%的阈值比较低（约7/16）。传统从格要求"全局克泄耗"，45%可能产生误判——一个命局可能有45%的官杀，但其他55%是印比日主，仍不成从格。
- **更合理的方式**：除了百分比，还应检查日主的根气。日主在地支有根（哪怕是微弱长生之根），就不能轻易从。

#### 7. 神煞多位置匹配丢失 (shensha.py L362-366)

```python
if shen_name not in result:
    result[shen_name] = {...}
```

当同一个神煞出现在多个位置时（如天乙贵人同时在年支和日支），代码只记录第一次匹配的位置。丢失的信息包括：
- 天乙贵人出现在多个柱，其力量远大于单柱
- 无法区分神煞具体落位（年/月/日/时柱各有不同含义）

#### 8. 大运起运岁数计算精度不足 (dayun.py)

```python
total_days = total_seconds / 86400.0
years_part = total_days / 3.0  # 直接除以3
```

传统算法：3天=1岁，1天=4个月，1个时辰(2小时)=10天。当前代码将总天数直接除以3，得到十进制年数如"3.4岁"。

问题：
- 传统命理中起运精确到"X岁X个月"，而不是十进制年
- 余数部分没有转化为月数（0.4岁=4.8个月，但代码只显示3.4岁）
- 节气发布日期使用基准日期+简单偏移（YEAR_TERM_OFFSETS），大量年份没有修正记录，可能导致节气日期偏差1天，影响起运计算

#### 9. 藏干权重不一致问题 (多模块)

系统中多处使用藏干权重但标准不一：
- **wuxing.py** (L97-106): 本气=1.5, 主藏干=0.5, 余气=0.2
- **shisheng.py** (L133-141): 本气=0.7, 余气=0.3
- **predictions.py** (L172-186): 每个藏干+1或-1，无权重区分

权重不一致导致analyze_wuxing_distribution和analyze_all_shi_shen的category_counts结果差异，进而影响从格百分比的计算。

#### 10. 得分系统中身弱和身强对称打分不合理 (predictions.py L129-137)

```python
base_by_strength = {
    "从强": 4, "从弱": 4,
    "身强": 3, "身弱": 3,
    "偏强": 2, "偏弱": 2,
    "中和": 1,
}
```

身强和身弱在传统命理中的含义不对称：
- 身强者可担财官，能克泄耗
- 身弱者喜生扶，忌克泄耗
- 同样的基础分（都是3分）抹杀了这种本质区别，导致评分对某些命局可能误导

---

### 🟢 建议优化问题

#### 11. 地支刑冲关系检测不全 (dayun.py + predictions.py)

- 三刑检测 (predictions.py L94): 只检查了 `{寅,巳}`, `{巳,申}`, `{寅,申}` 的组合，但没有检查完整的寅巳申三刑（需同时出现三个才叫三刑，两两出现不算真正的三刑）
- 漏掉了：丑戌未三刑、子卯刑、辰午酉亥自刑
- 流年分析 (dayun.py L274) 中的刑太岁检测 `DZ_SAN_XING[year_zhi] == ln_zhi` 也只检查了部分组合

#### 12. 月干五行生克微调逻辑缺陷 (predictions.py L165-170)

```python
if WU_XING_SHENG.get(month_gan_wx) == ri_wx:
    us_score = min(3, max(0, us_score + 1))  # 月干生日主 → 加1分
elif WU_XING_KE.get(month_gan_wx) == ri_wx:
    us_score = min(3, max(0, us_score - 1))  # 月干克日主 → 减1分
```

问题：用神分是综合判断（用神匹配+生克微调），但这里的生克微调对忌神时"克日主"也减分。如果月干是忌神的七杀克日主，固然为凶；但如果月干是七杀但为用神，七杀克日主反而可能激发日主潜力。当前逻辑一刀切。

#### 13. 男女命"官杀混杂"判定不当 (contextual.py L516-517)

```python
if gf + qs >= 2:
    love_parts.append("官杀混杂→感情经历丰富...")
```

对女命，"官杀混杂"是指同时有正官和七杀（且至少各一个），而不是总数≥2。两个正官或两个七杀不算传统意义上的"官杀混杂"。

同样男命"比劫夺财"的判断 (L456-457)：
```python
if bi >= 3 and cai >= 2:
```
阈值设定缺乏理论依据。

#### 14. 羊刃计算包含阴干 (constants.py L486-489)

```python
"羊刃": {
    "甲": ["卯"], "丙": ["午"], "戊": ["午"],
    "庚": ["酉"], "壬": ["子"],
    "乙": ["辰"], "丁": ["未"], "己": ["未"],
    "辛": ["戌"], "癸": ["丑"],
}
```

传统羊刃仅对阳干有效：甲刃在卯、丙戊刃在午、庚刃在酉、壬刃在子。阴干的"羊刃"（乙刃辰、丁刃未等）是《三命通会》中记载的"阴刃"，但非主流共识，在很多命理体系中不被接受。建议添加说明或仅限阳干。

#### 15. 流年"三刑太岁"检测不完整 (dayun.py L274-275)

```python
if year_zhi in DZ_SAN_XING and DZ_SAN_XING[year_zhi] == ln_zhi:
    tai_sui_relations.append("刑太岁")
```

DZ_SAN_XING 映射如 `"寅": "巳"`, `"巳": "申"`, `"申": "寅"`。这只检查了一对一的关系。但三刑的完整逻辑：
- 寅巳申三刑：需流年值寅/巳/申，原局有另外两个中的至少一个
- 丑戌未三刑：需流年值丑/戌/未，原局有另外两个中的至少一个
- 子卯刑：值子/卯即刑
- 辰/午/酉/亥自刑：需见两个相同地支

#### 16. 评分系统的季节分过于简化 (predictions.py L190-202)

```python
SEASON_STRENGTH = {
    "寅": {"木": 2}, "卯": {"木": 3}, ...
}
season_score = 0
for wx, strength in seasonal.items():
    if wx == ri_wx:
        season_score = 2  # 日主当令=2分
    elif wx in useful_god:
        season_score = 1  # 用神当令=1分
    break  # 只检查第一个匹配
```

`SEASON_STRENGTH` 每个地支只列出一个当令五行，但辰戌丑未月是土当令且有余气（辰有余气木水，戌有金火等）。当前只用第一个匹配，忽视了藏干中的其他当令五行。

#### 17. 空亡完全缺失但神煞列表中有声明 (shensha.py + constants.py)

shensha.py L279-283 声明了六甲空亡但 rule 为空字典。constants.py 中也没有空亡查找表。

**作为最小修复**，应至少实现：
```python
# 每旬的空亡地支
XUN_KONG_WANG = {
    "甲子": ["戌", "亥"], "甲戌": ["申", "酉"], "甲申": ["午", "未"],
    "甲午": ["辰", "巳"], "甲辰": ["寅", "卯"], "甲寅": ["子", "丑"],
}
```

#### 18. 知识库加载后未被用于LLM Prompt (knowledge_loader.py)

knowledge_loader.py 虽然提供了 `load_all_knowledge()` 和 `load_knowledge_by_topic()` 函数，但**搜索代码中没有找到任何地方调用这些函数**（除了 `get_shi_shen_description`）。

Prompt 文件虽然说要查知识库，但技术实现上知识库内容并没有注入到AI的系统中。LLM 只能依靠训练记忆中的知识，无法实时从项目知识库中检索。

#### 19. 提示词和代码实际功能不一致 (prompts/)

提示词中描述的能力远超代码实际实现：
- 提示词要求计算日干支用公式：`A = (年尾两位数+7)×5 + 15 + (年尾两位数+19)/4`，但代码实际使用儒略日算法
- 提示词要求考虑调候用神、穷通宝鉴，代码未实现
- 提示词要求六十四卦起卦、解梦，这些功能无代码支持
- 提示词要求神煞推算，实际上神煞已经由代码完成，但提示词中让LLM重新计算，可能产生冲突

#### 20. "杂气格"缺失 (wuxing.py)

辰戌丑未月（四季末）被称为"杂气月"，因为藏干多且杂。传统命理对杂气月有特殊格局判断方法：
- 杂气财官格：辰戌丑未月，藏干中的财星或官星透干
- 当前代码按照普通格处理（月令藏干透干定格局），没有特殊处理

---

### 修复建议（按优先级）

| 优先级 | 问题 | 建议修复方式 |
|--------|------|-------------|
| **P0** | 从格检测双层逻辑不一致 | 重构 wuxing.py：analyze_ri_zuo_strong_weak 只做强弱不管从格；analyze_ge_ju 做所有从格/格局判断 |
| **P0** | 调候用神缺失 | 新增调候用神表（参考穷通宝鉴），加入用神判断流程；调候优先于强弱用神 |
| **P1** | 空亡系统缺失 | 在 constants.py 中增加空亡查找表，在 shensha.py 中实现空亡推算 |
| **P1** | 化气格月令过于严格 | 扩展化气月令至生助化神的月份（如甲己化土允许巳午月） |
| **P1** | 藏干权重不一致 | 统一全系统的藏干权重体系 |
| **P2** | 从格缺失从势格/假从 | 增加"从势格"检测，所有从格增加"假从"分支 |
| **P2** | 神煞多位置丢失 | 修改 result 数据结构支持同一神煞多位置 |
| **P2** | 大运起运精确度 | 将起运算法改为 3天=1岁 带余数转月数 |
| **P2** | 刑冲关系检测不全 | 完善三刑、自刑检测逻辑 |
| **P3** | 羊刃阴干问题 | 羊刃仅限阳干或添加注释说明 |
| **P3** | 杂气格检测缺失 | 对辰戌丑未月增加杂气格判断分支 |
| **P3** | 官杀混杂判定修正 | 改为"正官和七杀各自≥1" |
| **P3** | 知识库注入LLM管道 | 在generate_fortune_report中将知识库内容注入到LLM请求中 |